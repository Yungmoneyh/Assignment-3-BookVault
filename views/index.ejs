<%- include('partials/header') %>

<!-- Hero section on homepage -->
<div class="hero">
  <h1>Welcome to BookVault</h1>
  <p class="lead">Track and share your reading — clean, private, and simple.</p>

  <div class="hero-buttons">
    <!-- Button to browse public books -->
    <a class="btn primary" href="/books/public">Browse Public Books</a>

    <!-- Show account button based on login status -->
    <% if (!user) { %>
      <a class="btn" href="/register">Create an Account</a>
    <% } else { %>
      <a class="btn" href="/books">View My Books</a>
    <% } %>
  </div>
</div>

<%- include('partials/footer') %>
